<div>
  {{#if giant}}
    <h1 class='mt-4 mb-4'>{{data}}</h1>
  {{else}}
    {{#if big}}
      <h2 class='mt-4 mb-3'>{{data}}</h2>
    {{else}}
      <h3 class='mb-3'>{{data}}</h3>
    {{/if}}
  {{/if}}
</div>

{{! <style>
  h2 { color:green; } h3 { color:green; }
</style> }}